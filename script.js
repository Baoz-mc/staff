function goToDetail(memberId) {
    window.location.href = `detail.html?id=${memberId}`;
  }

  document.addEventListener('DOMContentLoaded', () => {
    console.log("Trang team đã sẵn sàng!");

    // future animation hooks here
  });
